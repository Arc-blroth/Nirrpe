extern pure fn panic(x: any)
extern pure fn print(x: any)

fn factorial(x: u64): u64 {
    if x == 0 || x == 1 {
        return 1
    } else {
        return x * factorial(x - 1)
    }
}

print(factorial(10))
panic("nooooooooo")
